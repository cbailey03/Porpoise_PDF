[package]
name = "porpoise-app"
version = "0.1.0"
description = "Porpoise PDF viewer and editor."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "porpoise"
path = "src/main.rs"

[dependencies]
porpoise-doc.workspace = true
porpoise-view.workspace = true

# NOTE: eframe/egui arrive at M2, when the window does. Declared in the workspace
# manifest already so the version decision is made once.

[lints]
workspace = true
