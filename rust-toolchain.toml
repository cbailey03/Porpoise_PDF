# Pinned for reproducible local and CI builds. This is the *build* toolchain;
# the MSRV floor is `rust-version` in Cargo.toml and is checked by a separate CI
# job, so bumping this does not silently raise our minimum.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
